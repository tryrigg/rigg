---
title: Getting started
description: Install Rigg, initialize a project, and run your first workflow.
---

## Install

```bash
curl -fsSL https://tryrigg.com/install | bash
```

Specific version:

```bash
curl -fsSL https://tryrigg.com/install | bash -s -- --version v0.1.0
```

macOS only (prebuilt binary).

**Requirements:** `codex` on `PATH` for codex steps.

## Init and run

```bash
rigg init                              # creates .rigg/ with example workflows
rigg validate                          # check YAML syntax
rigg run hello --input name=Rigg       # run a workflow
```

`--input key=value` auto-parses JSON — `true`, `42`, `["a"]`, `{"x":1}` all work.

## Output

Interactive progress is shown on TTY. Non-TTY execution suppresses it automatically.

Run state is in-memory only; Rigg does not persist run history.
