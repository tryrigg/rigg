{
  "name": "@tryrigg/www",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "sync:install": "install -m 755 ../../install public/install",
    "dev": "bun run sync:install && astro dev",
    "build": "bun run sync:install && astro build",
    "check": "bun run sync:install && astro check",
    "preview": "bun run build && wrangler dev",
    "deploy": "wrangler deploy",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.1",
    "@astrojs/starlight": "^0.38.1",
    "@resvg/resvg-js": "^2.6.2",
    "astro": "^6.0.3",
    "satori": "^0.25.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.6",
    "@types/node": "^25.4.0",
    "typescript": "^5.9.3",
    "wrangler": "^4.72.0"
  }
}
